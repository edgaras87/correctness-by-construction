# Change-plan: absorb the handbook's 2026-09-01 reply

## Summary — the state after all commits

The reply is consumed and its temp file deletable: both adopted asks
acknowledged in our records, the two birth documents point at the
redesigned starter (installs/pure.md; the overlay contract's third
assumption in its new concrete form), TODO reflects the new ground —
handoff closed, the bundle-side set unblocked and reshaped to
include the playbook rebuild, the next handoff queued with the
injection-friction log and the default.md naming collision — and the
devlog carries the session. Deliberately deferred to the bundle-side
set: every line describing how the playbook is used (bundle table
row, Birth step 6), because the master is still middles-shaped until
that set rebuilds it — the projection rule applied to ourselves.

First set planned under the injected convention: the records walk
ran (PLAN: no step event; ADR: none here — the vendored-endpoints
decision is the content set's, landing Proposed there; CHANGELOG: no
concept bump; ARCHITECTURE: shape unchanged, the coarse executions
row covers new files inside the component; README: nothing newly
outside-true; decisions.md: carried by the hand-move commits).

## Commits

**1. `docs(executions): re-aim birth docs at new starter`**
birth-scenario.md's seed points at starter/installs/pure.md instead
of starter/README.md; the bundle README's contract paragraph takes
the redesigned third assumption (the STEPS-marker region and
playbooks/default.md, per their ADR-0028) and re-cites the contract.
Only what is true now — playbook-usage lines wait for the rebuild.

**2. `docs: retriage TODO after the handbook reply`**
The handoff Now item closes (delivered, replied, absorbed). The
bundle-side Next item unblocks and widens: design against the
slimmed stubs, plus the cbc-run playbook rebuilt as a full sequence
on playbooks/default.md's base — kit endpoint steps vendored with
provenance and pin, our harvested middles inserted unchanged, the
ownership decision landing as that set's Proposed ADR. The
playbook-overlap item takes the redesign's likely answer
(chosen-and-copied-whole). A new Now item queues the next handoff:
the injection friction log and the default.md naming collision
(two same-named files meaning opposite things — a direction and the
absence of one).

**3. `docs: add devlog entry for reply absorption`**
The session record: what the reply granted, the hand-move and its
friction notes gathered in one place, the default.md disambiguation
and the vendored-endpoints reasoning, the resume line (bundle-side
set next, then the re-birth). Temp files deleted after this commit.

## Decisions taken inside this plan

- Playbook-usage prose (bundle table row "copied at Framing", Birth
  step 6) stays as-is this set, though the redesign obsoletes it at
  the next birth: the master it describes is still middles-shaped,
  and the re-birth is sequenced after the rebuild anyway. Updating
  it now would describe a file that does not exist.
- The friction log lives in the devlog entry and the TODO handoff
  item, not its own artifact — it is handoff payload, not a record.
- No ADR in this set: the only decision of record (endpoint
  vendoring) belongs to the set that creates its material.
